run_name: Q2-final-B0-WCE
model_status: LOCKED
project_root: D:/华为杯/E2026
data:
  pkl_path: data/raw/aligned_50.pkl
  pkl_path_absolute: D:/华为杯/E2026/data/raw/aligned_50.pkl
  train_split: train
  valid_split: valid
  test_split: test
  batch_size: 128
  num_workers: 0
  sequence_length: 50
  feature_dimensions:
    text: 768
    audio: 74
    vision: 35
  padding_mask_source: text_bert[:,1,:] == 1
preprocessing:
  normalization: none
model:
  hidden_dim: 128
  fusion_dim: 128
  dropout: 0.1
  classification_classes: 3
  regression_outputs: 1
  architecture: B0Baseline
  pooling: masked_mean
  modality_projection: Linear -> LayerNorm -> GELU -> Dropout
  fusion: concat -> Linear(384,128) -> LayerNorm -> GELU -> Dropout
  heads:
  - Linear(128,3)
  - Linear(128,1)
training:
  seeds:
  - 42
  - 43
  - 44
  epochs: 80
  patience: 12
  learning_rate: 0.001
  weight_decay: 0.0001
  optimizer: AdamW
  classification_loss: balanced_train_weighted_cross_entropy
  regression_loss: SmoothL1
  lambda_reg: 1.0
  class_weighting: balanced_train
  augmentation: none
  evaluate_test: false
  checkpoint_selection: best_validation_selection_score
benchmark:
  definition: outputs/metrics/b2_benchmark_definition.json
  seed: 20260923
  sha256: 3dda8bcef01bb5eba005e4ac7b729cccec5c1943177066d7c82a15ad99205bff
  selection_score: 0.25*Accuracy + 0.25*MacroF1 + 0.25*(1-MAE/6) + 0.25*(Pearson+1)/2
  robust_score: 0.5*clean_selection_score + 0.5*mean_missing_selection_score
checkpoints:
  '42': outputs/checkpoints/b0_weighted_ce_best_selection_score.pt
  '43': outputs/checkpoints/b21_b0_seed_43_best_selection_score.pt
  '44': outputs/checkpoints/b21_b0_seed_44_best_selection_score.pt
